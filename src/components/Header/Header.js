import React from 'react'
import Clock from '../Clock/Clock'

const Header = (props) => {
    return (
        <header>
            <div>
                <Clock />
            </div>
        </header>
    )
}

export default Header